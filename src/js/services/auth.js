/* import firebase from 'firebase';


const gauthId = process.env.GAUTH_CLIENT_ID;
const ui = new firebaseui.auth.AuthUI(firebase.auth());

const uiConfig = {
  signInFlow: 'popup',
  signInSuccessUrl: '/',
  signInOptions: [firebase.auth.GoogleAuthProvider.gauthId]
};

ui.start('#firebaseui-auth-container', uiConfig);

export default ui; */
